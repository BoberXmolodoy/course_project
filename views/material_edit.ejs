<!DOCTYPE html>
<html>

<head>
    <title>Редагувати матеріал</title>
    <link rel="stylesheet" href="/css/new_material.css">
</head>

<body>
    <div class="container">
        <h1>Редагувати матеріал</h1>
        <form id="materialForm" action="/materials/<%= material._id %>" method="post">
            <div>
                <label for="name">Назва:</label>
                <input type="text" id="name" name="name" value="<%= material.name %>" required>
            </div>
            <div>
                <label for="quantity">Кількість:</label>
                <input type="number" id="quantity" name="quantity" value="<%= material.quantity %>" min="1" required>
            </div>
            <div>
                <label for="description">Опис:</label>
                <textarea id="description" name="description" required><%= material.description %></textarea>
            </div>
            <div>
                <label for="type">Тип:</label>
                <select id="type" name="type" required>
                    <option value="">Оберіть тип</option>
                    <option value="Зброя" <%=material.type==='Зброя' ? 'selected' : '' %>>Зброя</option>
                    <option value="Боєприпаси" <%=material.type==='Боєприпаси' ? 'selected' : '' %>>Боєприпаси</option>
                    <option value="Продовольство" <%=material.type==='Продовольство' ? 'selected' : '' %>>Продовольство
                    </option>
                    <option value="Обладнання" <%=material.type==='Обладнання' ? 'selected' : '' %>>Обладнання</option>
                    <option value="Медичні засоби" <%=material.type==='Медичні засоби' ? 'selected' : '' %>>Медичні
                        засоби</option>
                </select>
            </div>
            <button type="submit">Оновити</button>
        </form>
    </div>

    <script>
        document.getElementById('materialForm').addEventListener('submit', function (event) {
            const name = document.getElementById('name').value.trim();
            const quantity = document.getElementById('quantity').value.trim();
            const description = document.getElementById('description').value.trim();
            const type = document.getElementById('type').value;

            if (!name || !quantity || !description || !type) {
                alert('Будь ласка, заповніть усі поля форми.');
                event.preventDefault();
            }
        });
    </script>
</body>

</html>